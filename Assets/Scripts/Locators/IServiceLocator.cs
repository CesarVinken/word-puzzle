using UnityEngine;

// IServiceLocators are supposed to be (together with the GameManager) the only singletons in the architecture. We use them to locate other services
public interface IServiceLocator
{
    
}
